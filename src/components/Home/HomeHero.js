import Button from '../UI/Button';

export const HomeHero = () => {
    return (
        <div className="container py-12">
            <Button>
                <span>Click !</span>
            </Button>
        </div>
    );
};
